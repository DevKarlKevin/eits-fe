import {Routes} from '@angular/router';
import {TabContainerComponent} from '../measure/pages/tab-container/tab-container.component';

export const routes: Routes = [
  {path: '', component: TabContainerComponent}
];
